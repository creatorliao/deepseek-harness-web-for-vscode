# 待办总表（跨主题优先级）

**日期**: 2026-09-14 ｜ **状态**: 当前 ｜ **定位**: 本项目**唯一**的待办真源。各主题夹的 `NN-待办_*.md` 是当时的历史记录，不再作为排期依据。

**规则**: 完成一项 → 标记 ✅ 并注明证据；新增项 → 按优先级插入并给编号。编号 `G-xx` 沿用历史编号，不复用、不重排。

---

## P0 — 立即（阻塞发布或存在不可逆风险）

| # | 项 | 价值 / 风险 | 状态 | 证据 / 来源 |
|---|---|---|---|---|
| **G-17** | **真机验证（进行中）**：已把 v0.3.4 装进 **Cursor 3.16.29**（内嵌 VS Code 1.128.0）；待确认 ① 视图能正常渲染 DSH 完整 UI；② 窄栏下 DSH 自带侧边栏确实自动收成 56px；③ 窄栏里会话区是否可用（`CENTER_MIN=400`，窄栏会被压到 `max(0, viewport − 56)`）；④ VS Code 1.105.1 下 F5 是否正常；⑤ Antigravity 是否支持 `secondarySidebar`（需其 VS Code 基线 ≥ 1.105） | 代码与静态验证全绿、Cursor 安装成功，但**尚未在编辑器里实际看过界面** | ⏳ 待用户实测 | [../R20260914-02-DSH内嵌原理与教程/05-方案_面板常驻右侧.md](../R20260914-02-DSH内嵌原理与教程/05-方案_面板常驻右侧.md) §0.5 ｜ [../R20260817-01-桥架构与IDE内嵌/13-修复_dsh启动超时与自动探测失效.md](../R20260817-01-桥架构与IDE内嵌/13-修复_dsh启动超时与自动探测失效.md) |
| **G-18** | **Archived 会话与视图绑定的边界**：归档当前绑定到侧边栏视图的会话后，视图仍指向它（编辑器标签页形态会关掉对应面板） | 边界行为不一致，需决定"归档时自动切到另一个会话"还是"保持不动并提示" | ⏳ 未开始 | `src/chatView.ts` 与 `src/extension.ts` 的 `onArchiveSession` |
| **G-13** | **Marketplace 整改与换名再上架**：① 回信确认冷却期精确起止日与具体违规条款；② 确定新扩展名策略（Marketplace 专用新 id）；③ 执行整改清单（新增 `SECURITY.md`、publisher 域名验证 `isDomainVerified:false→true`、代码签名、软化剪贴板描述）；④ 冷却期满（约 2026-09-25 后）提复审；⑤ 复审通过后吊销 PAT / 评估 Entra ID（PAT 2026-12-01 退役） | **原扩展名永久作废**；不做就只能靠 GitHub 手动分发 vsix | ⏳ 未开始（5 条子项全未执行；`SECURITY.md` 不存在） | [../R20260826-01-发布与分发/01-事件_Marketplace被移除.md](../R20260826-01-发布与分发/01-事件_Marketplace被移除.md) |
| **G-14** | **上游兼容巡检机制落地**：`docs/02-Areas/dsh-baseline.json` + `npm run check:dsh` + 定时 CI 巡检已建；**每次上游发新版必须跑一次并落档** | 上游约一个月走完 rc.6→0.1.5，无机制就会重演"用户升级后白屏" | ⏳ 机制已建，待首次实战检验 | [../../02-Areas/20260914-06-上游兼容规范_跟随最新官方DSH.md](../../02-Areas/20260914-06-上游兼容规范_跟随最新官方DSH.md) |
| **G-23** | **重启内核后复核 dsh `0.1.5-rc.2` 四破坏面**：本机全局 CLI 已升到 rc.2（next 最新），但 A–D 破坏面、`node scripts/smoke.js`、真机面板**都未复核**；任一面不过即回退 `0.1.5-rc.1` | 不复核＝"已跟上最新"是自封的；本次升级已暴露"热升级"风险 | ⏳ 未开始（阻塞于：需重启内核才有可用的命令通道） | [../R20260820-01-上游DSH版本适配/11-事件_热升级内核导致会话工具链失效.md](../R20260820-01-上游DSH版本适配/11-事件_热升级内核导致会话工具链失效.md) ｜ [../../02-Areas/20260915-01-内核升级最佳实践.md](../../02-Areas/20260915-01-内核升级最佳实践.md) |
| **G-24** | **目录树拖拽的真机验收 + 失败预案**：2026-09-17 真机反馈——① 右键入口**可用**（但暴露"重复两条"缺陷，已修 v0.5.2）；② **拖拽实测不生效**（未确认是否按住 Shift）；③ 待补：**按住 Shift 复测**；若仍无效 → 启用 E3（侧边栏 TreeView 投放区，官方支持且宿主直接拿到 DataTransfer）；④ 顺带核对 Cursor 1.128.0 的 Shift 放行逻辑 | 拖拽是唯一"代码完成但未经人手验证"的一跳；页面侧链路已由真机探针证明正常（`09-事实` §真机补充）；等价右键入口已可用，故不阻塞发布 | ⏳ 进行中（阻塞于：需人手真实鼠标拖拽） | [../R20260917-01-目录树拖拽到Composer/07-待办_目录树拖拽到Composer.md](../R20260917-01-目录树拖拽到Composer/07-待办_目录树拖拽到Composer.md) ｜ [11-修复](../R20260917-01-目录树拖拽到Composer/11-修复_右键插入报失败导致重复两条引用.md) |

## P1 — 中期

| # | 项 | 价值 | 状态 | 证据 |
|---|---|---|---|---|
| **G-25** | **定性"默认界面形态到底是哪个"**：代码/清单/单测三处都是 `sidebar`（`src/openTarget.ts` 的 `DEFAULT_OPEN_TARGET`、`package.json` 配置默认值、`test/openTarget.test.js`），但 `README` 与 `CHANGELOG[0.4.0]` 说默认 `editorTab`（0.4.0 提交标题也写 "embed DSH in the editor tab by default"） | 决定**改代码**（把默认改成 `editorTab`，1 行 + 清单 + 单测 + 中英 README 四处同步）还是**维持现状**；不改就会一直留着一处"读文档以为开在编辑器、实际开在侧边栏"的落差 | ⏳ 待用户定性（2026-09-17 已把 README 订正到与代码一致，CHANGELOG 历史条目按规范保留） | [04-文档勘误台账.md](04-文档勘误台账.md) B7 |
| **G-01** | 共享实例探测（cwd-aware attach 轻量版）：用户已手动起 `dsh web` 且 cwd 匹配时挂接/提示，不重复起进程。**已决策不做共享 dsh 服务**（每 IDE 各管各的） | 避免同工作区起两个 dsh | ⏳ 未开始 | 依赖 V-02 |
| **G-04** | CI 自动发布工作流（tag 触发）。**注意**：原模板为"双渠道"，Marketplace 半边已失效；模板 Actions 版本（`@v4`）低于现 `ci.yml`（`@v7`）、Secret 名 `OVSX_PAT` 与 Makefile 的 `OVSX_TOKEN` 不一致，落地前必须订正 | 把 `make publish` 手工步骤自动化 | ⏳ 未开始 | [../R20260826-01-发布与分发/02-待办_CI自动双发工作流模板.md](../R20260826-01-发布与分发/02-待办_CI自动双发工作流模板.md) |
| **G-05** | 上游 issue：嵌入式页面同步 host settings（locale 等）。主题已由 matchMedia shim 覆盖。**⚠️ 2026-09-15 更新**：G-20 已找到上游正规钩子 `__DSH_TRANSPORT__.ownsHost`（可让 settings 真正落盘），本项宜在 G-20 落地后重新评估是否还需要提上游 | 少一处本地绕行 | ⏳ 未开始（**优先提上游，本地不做**） | `src/` 无 locale/settings 镜像 ｜ [../R20260817-01-桥架构与IDE内嵌/14-修复_面板内图片不显示与内测声明反复出现.md](../R20260817-01-桥架构与IDE内嵌/14-修复_面板内图片不显示与内测声明反复出现.md) |
| **G-06** | 多语言 README / 详情页语言门：目前仅 `README.md` + `README.zh.md` 两语（9 语言壳层已就绪） | 与 9 语言 UI 对齐 | ⏳ 未开始 | — |
| **G-10** | **路径点击跳转**：聊天窗 Markdown 路径 → 在 IDE 打开文件/定位行 | 消除从聊天到代码的断层（外部评审列为 P0 体验项） | ⏳ 未开始 | `src/` 无 `ide-open-file` |
| **G-11** | **选区右键快捷提问**：选中代码/文件一键进 DSH 会话 | 消除跨窗口复制粘贴 | ⏳ 未开始 | `src/` 无 `ide-request-context` |
| **G-15** | **补齐 Feature Pipeline 空洞**：v0.3.1–v0.3.4 的工作（rc.8 预加载、`globalThis.__DSH_BOOT__`、侧边栏握手、dsh 0.1.2-rc.1 认证/Typert/dist）**只有修复记录、没有需求/方案/计划文档** | 里程碑无追溯，规格无法复算 | ⏳ 未开始（本轮已用主题夹 `R20260820-01` 归口，管线文档仍缺） | [04-文档勘误台账.md](04-文档勘误台账.md) |
| **G-19** | **嵌入面板图片不显示**（粘贴截图的缩略图与消息内图片都是破图）：CSP `img-src` 未放行 `blob:`，而上游图片预览全部用 Blob URL（截图里的 `image.png` 破图就是 `alt` 文本） | 每天都碰；修复是 `buildCsp` 里加一个 `blob:` | ✅ 代码已落地（**真机待验证**） | [../R20260817-01-桥架构与IDE内嵌/14-修复_面板内图片不显示与内测声明反复出现.md](../R20260817-01-桥架构与IDE内嵌/14-修复_面板内图片不显示与内测声明反复出现.md) |
| **G-20** | **嵌入面板 settings scope 停在 memory 模式**：「内测声明」每次弹（本机 `~/.dsh/settings.yaml` 其实已有 `ui-onboarding.welcomeNoticeVersion`，面板根本没读）；同根因还导致面板里的设置写入不落盘（含 Models 页保存） | 影响面大于弹窗本身；上游 `ClientTransportHooks.ownsHost` 是正规钩子（我们本来就是 postMessage 隧道） | ✅ 代码已落地（**真机待验证**；含"设置开始落盘"的行为变更） | 同上 |
| **G-22** | **把内核升级 SOP 自动化**：`scripts/probe-dsh-surfaces.js`（四破坏面 A–D 的机器断言）+ `scripts/upgrade-dsh.js`（检查 → 停会话提示 → 装 → 重启提示 → canary → 打印待登记清单） | 把"人肉核对 + 靠记忆"变成一条命令；本次事故正因 SOP 只在规范里、没在工具里 | ⏳ 未开始 | [../../02-Areas/20260915-01-内核升级最佳实践.md](../../02-Areas/20260915-01-内核升级最佳实践.md) §9/§11 |
| **G-21** | **侧边栏入口与版式重排（2026-09-15 用户提出）**：① 新增第三个入口「在浏览器中打开」（命令能力已有、缺侧边栏入口）；② 运行状态 / 更新提示 / 停止下沉到底部；③ 打开入口成组（一主两次）+ 文案左对齐 + 前置图标；④ 会话区上移、"新建会话"并入标题行 | 现状是"高频动作被低频信息压在下面 + 五个全宽居中按钮层级不分"（用户截图评审）；日常使用每次都要多点一次 | ⏳ 需求与方案已写，**待用户批准后实施** | [../R20260915-01-侧边栏入口与打开方式/00-README.md](../R20260915-01-侧边栏入口与打开方式/00-README.md) |

## P2 — 低优先 / 观察

| # | 项 | 说明 | 倾向 |
|---|---|---|---|
| **G-07** | 下载流式转发（避免大导出文件整包内存缓冲） | `src/bridgeCore.ts` 仍全缓冲 `arrayBuffer()` | 多数用户无感，倾向关闭 |
| **G-08** | AbortController 传递（取消请求真正中断宿主 fetch） | 仅 `src/bridgeHost.ts` 的 `http-abort` 分支 | 30s 超时已兜底，倾向关闭 |
| **G-09** | 主题重载兜底（主题切换若 DSH 不 live-apply 则强制重载） | 需先验证 DSH live-apply 行为 | 见 V-01 |
| **M5** | 多会话并排（同工作区不同模型双面板对比） | 编排增强 | 待排期 |
| **G-16** | `upgrade-info` 死处理器：`launcherView.ts` 有分支但 `src/` 无发送方 | 历史残留 | 顺手清理即可 |

## 已知限制（已决策不修，如实登记）

| 限制 | 说明 | 决策来源 |
|---|---|---|
| 双 IDE 同工作区 + 同会话写冲突 | 两个 IDE 打开同一文件夹并共用同一会话时可能互相覆盖 | [../R20260819-01-工作区对齐/05-验证_工作区对齐.md](../R20260819-01-工作区对齐/05-验证_工作区对齐.md) |
| 平铺布局 Reload 后空面板占位（Antigravity） | 堆叠布局无此问题 | [../R20260819-02-多会话管理/05-验证_多会话管理.md](../R20260819-02-多会话管理/05-验证_多会话管理.md) |
| 无 `session.delete` / 无 unarchive | DSH 归档为 append-only，会话不删除、可恢复 | [../R20260819-02-多会话管理/07-待办_多会话管理.md](../R20260819-02-多会话管理/07-待办_多会话管理.md) |
| 版本硬门：`dsh < 0.1.2-rc.1` 拒绝启动 | 旧版 dsh 说的是 0.1.2 之前的 API，不再支持 | `src/serverManager.ts` `MIN_DSH_VERSION` |

## 待验证（决策前置）

| # | 验证项 | 影响 |
|---|---|---|
| V-01 | DSH 主题切换是否 live-apply（不开面板重载） | 决定 G-09 关闭或实现 |
| V-02 | `host.describe.cwd` 对 attach 探测的可靠性（多实例并存） | 决定 G-01 的探测策略 |
| V-03 | 双 IDE 同工作区写冲突的真实触发面 | 决定是补文档还是补机制 |

---

*关联文档：[02-路线图.md](02-路线图.md) ｜ [01-演进时间线.md](01-演进时间线.md) ｜ [04-文档勘误台账.md](04-文档勘误台账.md)*
