# 演进时间线 —— 扩展版本 × 上游 dsh 版本

**日期**: 2026-09-14 ｜ **状态**: 当前 ｜ **定位**: 一张表看清"上游什么时候变了、我们什么时候跟上的"。上游 dsh 迭代极快，本文件是判断"我们落后了没有"的第一入口。

> 每行都可追溯到证据：上游破坏点 → `01-Projects/R20260820-01-上游DSH版本适配/` 下的修复记录 → commit。
> **怎么跟上下一个版本**：[../../02-Areas/20260914-06-上游兼容规范_跟随最新官方DSH.md](../../02-Areas/20260914-06-上游兼容规范_跟随最新官方DSH.md)
> **当前适配基线（机器可读）**：[../../02-Areas/dsh-baseline.json](../../02-Areas/dsh-baseline.json)

---

## 1. 上游 dsh 变更 × 本项目适配事件

| # | dsh 版本 | 破坏性 | 破坏点（具体） | 修复的扩展版本 | 证据 |
|---|---|---|---|---|---|
| 0 | `0.1.0-rc.6` | — | 基线：Phase 0 实测与 MVP 开发所依据的版本 | v0.0.1 – v0.2.0 | [../R20260817-01-桥架构与IDE内嵌/08-事实_Phase0实测笔记.md](../R20260817-01-桥架构与IDE内嵌/08-事实_Phase0实测笔记.md) |
| 1 | `0.1.0-rc.8` | ⚠️ 是 | ① `dsh-client-modules` 新增**阻塞式 preload** `<script src="/plugins/…">`（rc.6/rc.7 无）→ 面板报 "Failed to load plugins / HTML did not preload"；② `dsh-web-app` 新增 `openBrowser` 默认 true（`--no-open` 首次出现）→ 启动即弹外部浏览器；③ npm `next` 有 rc.8 而 `latest` 仍 rc.7 → 升级提醒看不到。**注意**：本机 CLI 仍显示 rc.7，是**传递依赖**解析到 rc.8，故按 CLI 版本判断会误判 | **v0.3.1** | [../R20260820-01-上游DSH版本适配/08-修复_dsh0.1.0-rc8破坏.md](../R20260820-01-上游DSH版本适配/08-修复_dsh0.1.0-rc8破坏.md) ｜ commit `1738922`（前置门控 v0.3.0 `a299534`） |
| 2 | `0.1.1-rc.2` | ⚠️ 是 | boot 注入形态变更：`window.__DSH_BOOT__ = {…}` → `globalThis["__DSH_BOOT__"] = {…}`；`BOOT_RE` 失配 → `rewriteBootPluginUrls` 原样返回 → plugin bundle URL 保持相对 → "bundle script /plugins/… failed to load"（首个失败 `@deepseek-ai/dsh-typert-registry`） | **v0.3.2** | [../R20260820-01-上游DSH版本适配/09-修复_dsh0.1.1-rc2-boot形态.md](../R20260820-01-上游DSH版本适配/09-修复_dsh0.1.1-rc2-boot形态.md) ｜ commit `1a8e3ec` |
| — | *（上游无变更）* | — | 侧边栏状态竞态（webview 消息早于页面监听器注册被丢弃） | v0.3.3 | [../R20260817-01-桥架构与IDE内嵌/12-修复_侧边栏状态卡在Starting.md](../R20260817-01-桥架构与IDE内嵌/12-修复_侧边栏状态卡在Starting.md) ｜ commit `9a942bc` |
| 3 | `0.1.2-rc.1` | ⚠️⚠️ **大重构** | 三层同时破坏：① 启动行变 `dsh web: http://127.0.0.1:<port>/?token=<launchToken>`，`/` 与全部 `/api`（含 WS 升级 `/api/remote.mux`）要求 authority 绑定签名 Cookie（`dsh-auth-<sha256(authority)>`）→ 纯 Node 请求全部 **401**；② RPC 改 Typert：`POST /api/<namespace>/<method>` + `{args:{…}}` 信封，`/api/workspace.list` → **404**（改走 `workspace/follow` 流基线）；`session.list` 的 `agentPreset` 挪进 `projections.values`；③ dist 改相对引用 `./assets/…` + `<base href="/">`，boot 新增 `batches[]`，preload 变 `/plugins/??pkg/client.js,…&rev=` mux 形态。**自本版起设硬版本下限**：`dsh < 0.1.2-rc.1` 直接拒绝启动（`src/serverManager.ts` `MIN_DSH_VERSION`） | **v0.3.4** | [../R20260820-01-上游DSH版本适配/10-修复_dsh0.1.2-rc1认证与RPC.md](../R20260820-01-上游DSH版本适配/10-修复_dsh0.1.2-rc1认证与RPC.md) ｜ commit `63ed0f0`（发版 `69e895a`、CI 适配 `aa6edcf`） |
| 4 | `0.1.5-rc.1`、`0.1.5-rc.2` | ✅ 无 | 实测通过，**无需改代码** | v0.3.4（沿用） | commit `7918816`（仅 README.md / README.zh.md 的兼容矩阵） |
| 5 | `0.1.5-rc.2`（**CLI 包本身**） | — | 本机全局 CLI 由 `0.1.5-rc.1` 升到 `0.1.5-rc.2`（next 通道最新）。**四破坏面 A–D 复核待做**——升级动作在活跃会话上执行，该会话工具链随即失效 | 待做（阻塞于重启内核） | [../R20260820-01-上游DSH版本适配/11-事件_热升级内核导致会话工具链失效.md](../R20260820-01-上游DSH版本适配/11-事件_热升级内核导致会话工具链失效.md) ｜ [../../02-Areas/20260915-01-内核升级最佳实践.md](../../02-Areas/20260915-01-内核升级最佳实践.md) |
| 6 | `0.1.5-rc.2`（**本机全局安装**） | — | 第 5 行的**续发事故**：07:06 那次 `@next` 安装被运行中的 `dsh web`（PID 3216，9/14 21:38 启动）独占锁住 `koffi.node`，npm 以 `EBUSY` 失败退出（`exit -4082`），树被留成 **koffi JS 3.3.0 + 原生 3.2.1** → 此后**每次** `dsh web` 启动即 `Mismatched native Koffi modules` 退码 1，而 `dsh --version` / `dsh web --help` 仍一切正常。扩展侧症状：面板只报 `dsh exited before ready (code=1, signal=null)`，**不含任何原因** | **v0.4.0**（代码侧只修可诊断性；环境侧停进程 + 显式版本重装） | [../R20260820-01-上游DSH版本适配/12-修复_全局dsh半装koffi错配导致启动即退码1.md](../R20260820-01-上游DSH版本适配/12-修复_全局dsh半装koffi错配导致启动即退码1.md) |

| 7 | `0.1.6-alpha.1`、`0.1.6-alpha.2` | ✅ 无 | 官方**首次进入 `0.1.6` 系列**（alpha 通道，2026-09-15 / 09-17 发布；`latest`/`next` 至今仍停在 `0.1.5-rc.2`）。2026-09-18 在隔离目录逐面核查：**四破坏面 A–D 形态全同**、baseline 9 个脆弱点全在、`scripts/smoke.js` 两版本均 exit=0 → **无需改代码**；依赖树 520 → 488 包（新增 58 / 移除 90）。**本轮未跟进**（alpha 不进基线，是否跟进待用户决定） | 待定（未跟进） | [../R20260918-04-上游0.1.6-alpha差距与升级/03-分析_0.1.5-rc.2到0.1.6-alpha.2差距.md](../R20260918-04-上游0.1.6-alpha差距与升级/03-分析_0.1.5-rc.2到0.1.6-alpha.2差距.md) ｜ [../../03-Resources/20260918-01-官方DSH发布说明_0.1.5-rc.2到0.1.6-alpha.2.md](../../03-Resources/20260918-01-官方DSH发布说明_0.1.5-rc.2到0.1.6-alpha.2.md) |

### 记录缺口（诚实登记，未验证 ≠ 无破坏）

| 区段 | 状态 |
|---|---|
| `0.1.0-rc.6 → rc.7` | 无记录 |
| `0.1.1-rc.3 → rc.6` | **完全无记录**，是否破坏未验证 |
| `0.1.2-rc.2 → 0.1.5-rc.1` | 只在 README/CHANGELOG 登记"实测通过"，无逐版核查记录 |

## 2. 扩展自身版本演进（交付了什么）

| 版本 | 日期 | 交付 | 主题夹 |
|---|---|---|---|
| `0.0.1` | 2026-08-17 | MVP：spawn `dsh web`、传输桥（fetch/WS/剪贴板）、编辑器标签页 webview、侧边栏启动器、状态栏、主题同步、打包 | [R20260817-01](../R20260817-01-桥架构与IDE内嵌/00-README.md) |
| `0.0.2` | 2026-08-17 | 修：把运行时依赖 `ws` 打进 vsix（否则激活崩溃） | 同上（`11-修复_vsix缺少ws依赖.md`） |
| `0.0.3` | 2026-08-17 | 中英双语字符串表；marketplace 描述改纯英文 | — |
| `0.0.4` | 2026-08-17 | 编辑器标签页 webview 的 DeepSeek 图标 | — |
| `0.0.5` / `0.0.6` | 2026-08-17 | 扩展 id / displayName 改名（Marketplace 名称全局唯一） | — |
| `0.0.7` | 2026-08-17 | 仓库改名后指向新 repo | — |
| `0.0.8` | 2026-08-17 | 跨平台 CI 矩阵（macOS/Ubuntu/Windows）+ 真实 dsh spawn 冒烟；修 Windows 二进制解析与 `shell:true` spawn | 同上（`10-修复_跨平台dsh解析与spawn.md`） |
| `0.0.9` | 2026-08-17 | Windows 进程树终止（`taskkill /T /F`）；单测跨平台 | — |
| `0.0.10` | 2026-08-17 | 9 语言 UI（ja/ko/ru/es/pt/fr/de） | — |
| `0.1.0` | 2026-08-18 | **工作区对齐**（workspace 锚点跟随 IDE）+ dsh 版本软校验与升级辅助 | [R20260819-01](../R20260819-01-工作区对齐/00-README.md) |
| `0.2.0` | 2026-08-19 | **会话管理器 + 多面板多会话** | [R20260819-02](../R20260819-02-多会话管理/00-README.md) |
| `0.3.0` | 2026-08-20 | **双渠道升级提示**（latest/next）+ `--no-open` 版本门控 | [R20260820-01](../R20260820-01-上游DSH版本适配/00-README.md) |
| `0.3.1` | 2026-08-20 | 修 dsh rc.8 破坏（插件预加载 / 自动开浏览器 / 预发布提示 / DisposableStore）+ 侧边栏显示扩展版本 | 同上 |
| `0.3.2` | 2026-08-20 | 修 dsh 0.1.1-rc.2 boot 形态变更 | 同上 |
| `0.3.3` | 2026-08-22 | 修侧边栏状态卡在 "Starting…"（页面就绪握手） | [R20260817-01](../R20260817-01-桥架构与IDE内嵌/12-修复_侧边栏状态卡在Starting.md) |
| `0.3.4` | 2026-09-07 | **适配 dsh 0.1.2-rc.1**（令牌→Cookie 认证、Typert RPC、dist 布局）+ 硬版本下限 + "Open in Browser" 开认证 URL | [R20260820-01](../R20260820-01-上游DSH版本适配/10-修复_dsh0.1.2-rc1认证与RPC.md) |
| *（未发版）* | 2026-09-10 | 登记 dsh `0.1.5-rc.1` / `0.1.5-rc.2` 实测通过（无代码变更） | — |
| `0.4.0`（未发版） | 2026-09-15 | 启动失败可诊断性（子进程**退出**分支补上 stderr 首条 error 行与尾部）等，完整清单见 [CHANGELOG.md](../../../CHANGELOG.md) | [R20260820-01](../R20260820-01-上游DSH版本适配/12-修复_全局dsh半装koffi错配导致启动即退码1.md) |
| `0.5.0` | 2026-09-17 | **目录树 → composer 的 `@` 引用**（拖拽 + 右键入口）：不依赖上游破坏面变更，但新增一个上游脆弱点（composer 的 `[data-composer-input]` 锚点 / paste 命令绑定 / `@` 语法） | [R20260917-01](../R20260917-01-目录树拖拽到Composer/00-README.md) |
| `0.5.1` | 2026-09-17 | **工程与文档加固**（无运行时改动）：装机脚本 `install:local`、文档门禁 `check:docs`、README 默认形态订正、环境说明纠错 | 同上 |
| `0.5.2` | 2026-09-17 | **修右键入口重复引用**（写入层用同步 DOM 读判定 vs Lexical 异步提交）；新增真机探针 `probe:composer` | [11-修复_右键插入报失败导致重复两条引用.md](../R20260917-01-目录树拖拽到Composer/11-修复_右键插入报失败导致重复两条引用.md) |
| `0.5.3` | 2026-09-17 | **入口形态换挡**（拖拽被 VS Code webview 屏蔽挡住）→ 快捷键 `Ctrl+Alt+A` 加入当前文件 | [02-需求 §7](../R20260917-01-目录树拖拽到Composer/02-需求_目录树拖拽到Composer.md) |

## 3. 渠道事件

| 日期 | 事件 | 证据 |
|---|---|---|
| 2026-08-26 | VS Code Marketplace 把本扩展**移除**（非下架）：publisher 被 VSID 并发锁拦截全部 Marketplace API，扩展名 `deepseek-harness-web-for-vscode` **永久作废**，publisher 进入 30 天冷却期，微软明确"移除的扩展不予恢复" | [../R20260826-01-发布与分发/01-事件_Marketplace被移除.md](../R20260826-01-发布与分发/01-事件_Marketplace被移除.md) |

## 4. 项目归属与命名沿革

| 日期 | 变更 |
|---|---|
| 2026-08-17（v0.0.5 / v0.0.6） | 扩展 id 由 `deepseek-harness-for-vscode` 改为 `deepseek-harness-web-for-vscode`、展示名改为 "DeepSeek Harness Web for VS Code" —— 因为 Marketplace 的扩展名与展示名都要全局唯一 |
| **2026-09-14** | **项目归属变更**：改由 `liaohai1` 维护，版权与署名归 `liaohai1`。扩展 id 改为 **`creatorliao.deepseek-harness-for-vscode`**，展示名改为 **DeepSeek Harness for VS Code**，仓库改为 `creatorliao/deepseek-harness-for-vscode`。同时完成文档体系 PARA 重构（`doc/` → `docs/`）、新增 `dist/` 单件构建产物约定、新增上游跟随机制 |
| **2026-09-14** | **界面形态改为右侧次级侧边栏**（`engines.vscode` 由 `^1.90.0` 抬到 **`^1.105.0`**，**1.90–1.104 不再支持**）：DSH UI 默认开在右侧次级侧边栏，编辑器标签页形态保留，由 `deepseekHarness.openTarget` 切换。同时确立兼容原则（**对外用最新 API 设计、但 `engines` 只取下限；对内尽量不让已有用户受损**），写入 [AGENTS.md §7.1](../../../AGENTS.md) |

> ⚠️ 2026-09-14 的改名对编辑器而言是**扩展 id 变更**（`publisher.name` 变了），旧 id 的安装与设置不会自动迁移。历史主题夹内仍保留旧名，因为那是**当时的事实**。

---

*关联文档：[02-路线图.md](02-路线图.md) ｜ [03-待办总表.md](03-待办总表.md) ｜ [../../02-Areas/20260914-06-上游兼容规范_跟随最新官方DSH.md](../../02-Areas/20260914-06-上游兼容规范_跟随最新官方DSH.md)*
